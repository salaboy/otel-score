name: Evaluate CNCF Project OTel Maturity

on:
  workflow_dispatch:
    inputs:
      project_url:
        description: 'CNCF project GitHub URL (e.g. https://github.com/jaegertracing/jaeger)'
        required: true
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  evaluate:
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ── Evaluation setup ─────────────────────────────────────────────────

      - name: Derive project name
        run: |
          PROJECT_NAME=$(basename "${{ github.event.inputs.project_url }}")
          echo "PROJECT_NAME=${PROJECT_NAME}" >> "$GITHUB_ENV"

      - name: Install kind
        run: |
          sudo curl -fsSL https://kind.sigs.k8s.io/dl/v0.24.0/kind-linux-amd64 -o /usr/local/bin/kind
          sudo chmod +x /usr/local/bin/kind
          kind version

      - name: Install helm
        run: |
          curl -fsSL https://get.helm.sh/helm-v3.17.3-linux-amd64.tar.gz -o /tmp/helm.tar.gz
          sudo tar -xzf /tmp/helm.tar.gz --strip-components=1 -C /usr/local/bin linux-amd64/helm
          helm version

      - name: Set up OTel evaluation cluster
        run: bash scripts/setup-otel-cluster.sh "${{ env.PROJECT_NAME }}"

      # ── Start otel-score ─────────────────────────────────────────────────
      # The container ships with kind, kubectl, helm, jq, and the Docker CLI.
      # --network=host is required so that kubectl inside the container can reach
      # the kind cluster API server at 127.0.0.1:<port> on the host; without it
      # 127.0.0.1 resolves to the container's own loopback and all kubectl calls fail.
      # The Docker socket is mounted so kind can create clusters on the host daemon.
      # /tmp is shared so the OTel Collector can write JSONL files the container
      # can later read back for evaluation.

      - name: Start otel-score
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OTEL_EXPORTER_OTLP_ENDPOINT: ${{ secrets.OTEL_EXPORTER_OTLP_ENDPOINT }}
          OTEL_EXPORTER_OTLP_HEADERS_API_KEY: ${{ secrets.OTEL_EXPORTER_OTLP_HEADERS_API_KEY }}
          DASH0_DATASET: ${{ secrets.DASH0_DATASET }}
        run: |
          docker run -d --name otel-score \
            --network=host \
            -e ANTHROPIC_API_KEY="${ANTHROPIC_API_KEY}" \
            -e OTEL_EXPORTER_OTLP_ENDPOINT="${OTEL_EXPORTER_OTLP_ENDPOINT}" \
            -e OTEL_EXPORTER_OTLP_HEADERS_API_KEY="${OTEL_EXPORTER_OTLP_HEADERS_API_KEY}" \
            -e DASH0_DATASET="${DASH0_DATASET}" \
            -v /var/run/docker.sock:/var/run/docker.sock \
            -v /tmp:/tmp \
            ghcr.io/${{ github.repository }}:latest

      - name: Wait for otel-score to be ready
        run: |
          echo "Waiting for otel-score on port 8080..."
          for i in $(seq 1 30); do
            if curl -s -o /dev/null http://localhost:8080/; then
              echo "Ready (attempt ${i})"
              exit 0
            fi
            echo "  ${i}/30 — sleeping 3s"
            sleep 3
          done
          echo "otel-score did not start within 90s"
          docker logs otel-score
          exit 1

      # ── Trigger evaluation ───────────────────────────────────────────────

      - name: Run evaluation
        run: |
          echo "Evaluating: ${{ github.event.inputs.project_url }}"
          curl -fsS -N -X POST http://localhost:8080/api/chat \
            -H "Content-Type: application/json" \
            -H "Accept: text/event-stream" \
            -d "{\"conversationId\": \"eval-${{ env.PROJECT_NAME }}\", \"clusterName\": \"otel-eval-${{ env.PROJECT_NAME }}\", \"message\": \"The kind cluster otel-eval-${{ env.PROJECT_NAME }} is already running with the OTel Collector and test backend deployed. Go straight to install CNCF project ${{ github.event.inputs.project_url }}, evaluate its OTel maturity, and generate the report.\"}" \
            --max-time 7200 | tee evaluation-response.txt

      # ── Extract results from container ────────────────────────────────────
      # The agent writes to /app/.otel-eval/ inside the container (ShellTools
      # runs inside the container, not on the host). Only /tmp is shared via
      # volume mount, so we need to docker cp the evaluation artifacts out.

      - name: Copy evaluation artifacts from container
        if: always()
        run: |
          docker cp "otel-score:/app/.otel-eval/${{ env.PROJECT_NAME }}" \
                    ".otel-eval/${{ env.PROJECT_NAME }}" 2>/dev/null || \
            echo "WARNING: could not copy .otel-eval from container"
          echo "── Files extracted from container:"
          find ".otel-eval/${{ env.PROJECT_NAME }}" -type f 2>/dev/null || echo "  (none)"

      # ── Artifacts ────────────────────────────────────────────────────────

      - name: Upload evaluation artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: otel-evaluation-${{ env.PROJECT_NAME }}
          path: |
            evaluation-response.txt
            .otel-eval/${{ env.PROJECT_NAME }}/
            /tmp/otel-eval-${{ env.PROJECT_NAME }}/
          if-no-files-found: warn

      # ── Open PR with results ─────────────────────────────────────────────

      - name: Collect results for PR
        id: collect
        run: |
          SRC=".otel-eval/${{ env.PROJECT_NAME }}"
          DST="results/${{ env.PROJECT_NAME }}"
          mkdir -p "$DST"

          FILES_FOUND=0
          for file in report.html EVALUATION.md RESEARCH.md INSTALL-PLAN.md; do
            if [[ -f "$SRC/$file" ]]; then
              cp "$SRC/$file" "$DST/$file"
              echo "  ✓ $file"
              FILES_FOUND=$((FILES_FOUND + 1))
            else
              echo "  ✗ $file (not generated)"
            fi
          done

          if [[ $FILES_FOUND -eq 0 ]]; then
            echo "ERROR: no evaluation files were generated"
            echo "── Container logs (last 50 lines):"
            docker logs otel-score --tail=50 2>/dev/null || true
            exit 1
          fi

          echo ""
          echo "── $FILES_FOUND file(s) will be included in the pull request:"
          ls -lh "$DST"
          echo "files_found=$FILES_FOUND" >> "$GITHUB_OUTPUT"

      - name: Create pull request with results
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: results/${{ env.PROJECT_NAME }}
          base: main
          commit-message: "results: add OTel maturity evaluation for ${{ env.PROJECT_NAME }}"
          title: "OTel maturity report: ${{ env.PROJECT_NAME }}"
          body: |
            Automated OTel maturity evaluation for [${{ env.PROJECT_NAME }}](${{ github.event.inputs.project_url }}).

            ## Included files
            - `report.html` — interactive radar chart report
            - `EVALUATION.md` — detailed per-dimension assessment with evidence
            - `RESEARCH.md` — research notes from project installation
            - `INSTALL-PLAN.md` — installation plan and configuration

            > Only files that were successfully generated are included.

            Triggered by @${{ github.actor }} via workflow dispatch.
          add-paths: results/${{ env.PROJECT_NAME }}/*

      - name: Retry PR on transient failure
        if: failure() && steps.collect.outputs.files_found != ''
        run: |
          echo "PR creation failed — retrying with git + gh cli"
          cd "$GITHUB_WORKSPACE"
          git checkout -B "results/${{ env.PROJECT_NAME }}"

          # Re-collect (the failed action may have left the worktree dirty)
          SRC=".otel-eval/${{ env.PROJECT_NAME }}"
          DST="results/${{ env.PROJECT_NAME }}"
          mkdir -p "$DST"
          for file in report.html EVALUATION.md RESEARCH.md INSTALL-PLAN.md; do
            [[ -f "$SRC/$file" ]] && cp "$SRC/$file" "$DST/$file"
          done

          git add "results/${{ env.PROJECT_NAME }}/"
          git -c user.name="github-actions[bot]" \
              -c user.email="github-actions[bot]@users.noreply.github.com" \
              commit -m "results: add OTel maturity evaluation for ${{ env.PROJECT_NAME }}" || true

          for attempt in 1 2 3; do
            echo "Push attempt ${attempt}/3..."
            if git push -f origin "results/${{ env.PROJECT_NAME }}" 2>&1; then
              break
            fi
            echo "Push failed — waiting 10s before retry"
            sleep 10
          done

          EXISTING=$(gh pr list --head "results/${{ env.PROJECT_NAME }}" --json number --jq '.[0].number' 2>/dev/null || true)
          if [[ -z "$EXISTING" ]]; then
            gh pr create \
              --title "OTel maturity report: ${{ env.PROJECT_NAME }}" \
              --body "$(cat <<'BODY'
          Automated OTel maturity evaluation for [${{ env.PROJECT_NAME }}](${{ github.event.inputs.project_url }}).

          ## Included files
          - `report.html` — interactive radar chart report
          - `EVALUATION.md` — detailed per-dimension assessment with evidence
          - `RESEARCH.md` — research notes from project installation
          - `INSTALL-PLAN.md` — installation plan and configuration

          > Only files that were successfully generated are included.

          Triggered by @${{ github.actor }} via workflow dispatch.
          BODY
          )" \
              --base main \
              --head "results/${{ env.PROJECT_NAME }}"
          else
            echo "PR #${EXISTING} already exists"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Print otel-score logs on failure
        if: failure()
        run: docker logs otel-score
